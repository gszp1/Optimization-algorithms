version: '3.8'

services:
  optimal_service:
    build:
      context: .
      dockerfile: Dockerfile 
    command: genetic 0 100 0 100 x^2 + y
    volumes:
      - ./results:/project/results
    stdin_open: true
    tty: true
